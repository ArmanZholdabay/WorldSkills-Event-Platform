# WorldSkills Event Platform

Платформа для управления событиями WorldSkills, построенная на Laravel 12. Система предоставляет функционал для организаторов событий и участников, включая управление событиями, сессиями, билетами, регистрациями и аналитику.

## 📋 Описание

WorldSkills Event Platform - это комплексная система управления событиями, которая позволяет:

- **Организаторам** управлять событиями, каналами, комнатами, сессиями, билетами и просматривать аналитические отчеты
- **Участникам** просматривать информацию о событиях, регистрироваться на сессии и оценивать их

## 🚀 Основные возможности

### Для организаторов
- ✅ Управление событиями (создание, редактирование, просмотр)
- ✅ Управление каналами событий
- ✅ Управление комнатами
- ✅ Управление сессиями (доклады, воркшопы)
- ✅ Управление билетами с различными типами (обычные, раннее бронирование, пакеты)
- ✅ Просмотр отчетов (заполняемость комнат и т.д.)
- ✅ Аутентификация организаторов

### Для участников (API)
- ✅ Просмотр информации о событиях
- ✅ Регистрация на события и сессии
- ✅ Просмотр своих регистраций
- ✅ Аутентификация участников
- ✅ Оценка событий и сессий

## 🛠 Технологический стек

- **Backend**: Laravel 12 (PHP 8.2+)
- **Frontend**: Blade шаблоны, Bootstrap
- **База данных**: MySQL
- **API**: RESTful API с Laravel Sanctum
- **Build Tool**: Vite

## 📁 Структура проекта

```
WorldSkills Event Platform/
├── laravel12/              # Основное Laravel приложение
│   ├── app/               # Модели, контроллеры
│   ├── database/          # Миграции, сидеры
│   ├── resources/views/   # Blade шаблоны
│   ├── routes/            # Маршруты (web, api)
│   └── public/            # Публичные файлы
├── files/                 # Вспомогательные файлы
│   ├── database/          # SQL дамп базы данных
│   ├── templates_admin-gui/  # HTML шаблоны админки
│   └── diagram_libs/      # Библиотеки для диаграмм
└── README.md              # Этот файл
```

## 📦 Требования

- PHP >= 8.2
- Composer
- Node.js и npm
- MySQL (рекомендуется MAMP для macOS)
- Git

## 🔧 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/ArmanZholdabay/WorldSkills-Event-Platform.git
cd "WorldSkills Event Platform"
```

### 2. Установка зависимостей PHP

```bash
cd laravel12
composer install
```

### 3. Настройка окружения

Создайте файл `.env` в директории `laravel12/`:

```bash
cp .env.example .env
```

Настройте параметры в `.env`:

```env
APP_NAME="WorldSkills Event Platform"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_TIMEZONE=UTC
APP_URL=http://localhost:8000

# Настройки базы данных (для MAMP)
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=8889
DB_DATABASE=worldskills
DB_USERNAME=root
DB_PASSWORD=root
```

### 4. Генерация ключа приложения

```bash
php artisan key:generate
```

### 5. Импорт базы данных

#### Автоматический импорт (рекомендуется)

```bash
php import-db.php
```

Или через bash скрипт:

```bash
./import-db.sh
```

#### Ручной импорт через phpMyAdmin

1. Откройте MAMP и запустите серверы
2. Откройте phpMyAdmin (http://localhost:8888/phpMyAdmin/)
3. Создайте базу данных `worldskills`
4. Импортируйте файл `../files/database/db-dump.sql`

### 6. Установка зависимостей Node.js

```bash
npm install
```

### 7. Запуск проекта

#### Вариант 1: Запуск всех сервисов одновременно

```bash
composer run dev
```

Это запустит:
- Laravel сервер (http://localhost:8000)
- Очередь задач
- Vite dev server

#### Вариант 2: Запуск по отдельности

**Терминал 1 - Laravel сервер:**
```bash
php artisan serve
```

**Терминал 2 - Vite dev server:**
```bash
npm run dev
```

**Терминал 3 - Очередь задач (если используется):**
```bash
php artisan queue:work
```

### 8. Открыть в браузере

Откройте: http://localhost:8000

## 📚 API Документация

### Базовый URL
```
http://localhost:8000/api
```

### Эндпоинты

#### Аутентификация участников
- `POST /api/v1/login` - Вход участника
- `POST /api/v1/logout` - Выход участника

#### События
- `GET /api/v1/organizers/{organizerSlug}/events/{eventSlug}` - Получить информацию о событии

#### Регистрации
- `POST /api/v1/organizers/{organizerSlug}/events/{eventSlug}/registration` - Зарегистрироваться на событие
- `GET /api/v1/registrations` - Получить список регистраций (требует аутентификации)

## 🔐 Аутентификация

### Организаторы
- Вход: `/organizer/login`
- После входа доступна админ-панель для управления событиями

### Участники
- Используйте API эндпоинт `/api/v1/login` для получения токена
- Токен используется в заголовке `Authorization: Bearer {token}`

## 📊 Структура базы данных

Основные таблицы:
- `organizers` - Организаторы
- `events` - События
- `channels` - Каналы событий
- `rooms` - Комнаты
- `sessions` - Сессии (доклады, воркшопы)
- `event_tickets` - Билеты на события
- `attendees` - Участники
- `registrations` - Регистрации на события
- `session_registrations` - Регистрации на сессии
- `event_ratings` - Оценки событий
- `session_ratings` - Оценки сессий

## 🧪 Тестирование

```bash
php artisan test
```

## 🔧 Полезные команды

### Очистка кеша
```bash
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
```

### Сброс базы данных
```bash
php artisan migrate:fresh
php artisan db:seed
```

### Создание символической ссылки для storage
```bash
php artisan storage:link
```

### Обновление автозагрузки классов
```bash
composer dump-autoload
```

## 🐛 Решение проблем

### Ошибка подключения к базе данных
- Убедитесь, что MAMP запущен
- Проверьте порт MySQL в MAMP (обычно 8889)
- Проверьте настройки в `.env`

### Ошибка "Class not found"
```bash
composer dump-autoload
```

### Ошибка с правами доступа
```bash
chmod -R 775 storage bootstrap/cache
```

## 📝 Лицензия

MIT License

## 👤 Автор

**Arman Zholdabay**

- GitHub: [@ArmanZholdabay](https://github.com/ArmanZholdabay)

## 🤝 Вклад в проект

Вклады приветствуются! Пожалуйста, создайте issue или pull request.

## 📞 Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории.

## 🔗 Полезные ссылки

- [Laravel документация](https://laravel.com/docs)
- [MAMP документация](https://documentation.mamp.info/)
- [Laravel Sanctum](https://laravel.com/docs/sanctum)

---

**Примечание**: Для подробной инструкции по запуску проекта см. файл `laravel12/START.md`

